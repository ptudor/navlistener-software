{
  "name": "navlisten-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build && vite build --ssr src/entry-server.js --outDir .static-ssr && node scripts/export-static.mjs && node scripts/check-static.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vue/server-renderer": "^3.5.39",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.4"
  }
}
