# Editor / language-server caches
.cache/
.vscode/
compile_commands.json

# ESP-IDF build artifacts
/build/
sdkconfig
sdkconfig.old
managed_components/
*.pyc
.DS_Store

# host-test binary the hardware_manifest suite builds beside its sources
components/hardware_manifest/test/hardware_manifest_policy_test
