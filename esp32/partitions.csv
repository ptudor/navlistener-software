# navfeeder-esp partition table (4 MB flash).
# Single factory app (OTA is future work) + NVS config + space reserved for a
# future LittleFS spool. The current spool component is RAM-only and does not
# mount or write this partition; overflow/reboot can therefore lose unacked
# records. The reserved partition is the planned equivalent of navfeeder.c's
# --spool-file disk tier, not an implemented durability guarantee.
# Name,      Type, SubType,  Offset,   Size
nvs,         data, nvs,      0x9000,   0x6000
phy_init,    data, phy,      0xf000,   0x1000
factory,     app,  factory,  0x10000,  0x200000
spool,       data, littlefs, ,         0x180000
