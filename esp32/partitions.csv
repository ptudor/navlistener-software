# navfeeder-esp partition table (4 MB flash).
# Single factory app (OTA is future work) + NVS config + a littlefs "spool" partition
# that survives reboots so an outage longer than the RAM ring is still lossless
# (mirrors navfeeder.c's --spool-file disk tier).
# Name,      Type, SubType,  Offset,   Size
nvs,         data, nvs,      0x9000,   0x6000
phy_init,    data, phy,      0xf000,   0x1000
factory,     app,  factory,  0x10000,  0x200000
spool,       data, littlefs, ,         0x180000
